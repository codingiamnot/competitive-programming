#include <bits/stdc++.h>

using namespace std;
int t,a,b;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    cin>>t;
    while(t)
    {
        t--;
        cin>>a>>b;
        cout<<a-1<<' '<<b<<'\n';
    }
    return 0;
}
